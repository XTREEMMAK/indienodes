# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Native hosts receive generated copies of the canonical web build.
/platforms/capacitor/android/app/src/main/assets/public/
/platforms/wails/frontend/dist/

# Miscellaneous
/static/

# Raw n8n workflow exports (scripts/n8n/build_workflows.py --export). Left
# exactly as the API returns them on purpose -- see scripts/n8n/backups/README.md
# -- so a diff against a later export is a diff against what n8n actually
# stored, not against prettier's opinion of it.
/scripts/n8n/backups/*.json
